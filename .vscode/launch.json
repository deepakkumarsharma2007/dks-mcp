{
    "version": "0.2.0",
    "configurations": [
         {
            "name": "Python Uvicorn MCP Server",
            "type": "debugpy",
            "request": "launch",
            //"program": "${file}",
            "module": "dks-mcp.server",
            //"program": "${workspaceFolder}/mcp/${file}",
            "console": "integratedTerminal",
            "args": "--host localhost --port 8001 --transport streamable-http",
            "envFile": "${workspaceFolder}/.env"
        }
    ]
}